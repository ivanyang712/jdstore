<div class="container">
  <div class="row">
    <div class="header">
      <div class="banner tc pt30 w1200 bc">
      <div id="myCarousel" class="carousel slide">
        <ol class="carousel-indicators">
          <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
          <li data-target="#myCarousel" data-slide-to="1"></li>
          <li data-target="#myCarousel" data-slide-to="2"></li>
       </ol>
       <div class="carousel-inner">
          <div class="item active">
           <a href="javacript:;">
            <img src="https://image.ibb.co/ftytiF/dark_375.png" >
            <div class="carousel-caption"></div>
           </a>
          </div>
          <div class="item">
           <a href="javacript:;">
            <img src="https://image.ibb.co/fm1DiF/milk_375.png">
            <div class="carousel-caption"></div>
           </a>
          </div>
          <div class="item">
           <a href="javacript:;">
            <img src="https://image.ibb.co/d9ZAVv/white_375.png">
            <div class="carousel-caption"></div>
           </a>
          </div>
        </div>
      </div>
    </div>
      <%= render "products/menu" %>
    </div>
 </div>

<div class="row">
    <% @products.each do |product| %>
      <div class="col-xs-6 col-md-3 tc">
        <div class="thumbnail productList-item">
        <% link_to product_path(product) %>
          <% if product.photos.present? %>
            <%= link_to image_tag(product.photos[0].avatar.thumb.url),product_path(product) %>
          <% else %>
            <%= link_to image_tag("http://placehold.it/200x200&text=No Pic", class: "img-ts"), product_path(product) %>
          <% end %>
        <br>
        <%= link_to(product.title, product_path(product)) %>
        <br>
        <strong class="col-red">￥ <%= product.price %></strong></p>
      </div>
    </div>
    <% end %>

  </div>

</div>
